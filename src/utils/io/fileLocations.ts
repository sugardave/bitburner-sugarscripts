const fileLocations = {
    nmapCache: {
        location: '/trove/maps',
        maps: ['all', 'owned', 'pwned'],
        suffix: '-servers.txt'
    }
};

export default fileLocations;
export {fileLocations};
